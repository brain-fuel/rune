// Code generated by goplus from circind.gp. DO NOT EDIT.
//goplus:v v0.15.0

package store

import "goforge.dev/rune/v3/core"

// The circle's DEPENDENT eliminator (§F / R-HIT / A9): the induction principle.
// Where circElim (store/hit.go) maps the circle into a fixed type (recursion),
// circInd eliminates into a DEPENDENT motive `P : El Circle -> UF` (induction) —
// the principle you prove things about the circle with. It is a SEPARATE
// one-member group on its own hash space (tag T/t) referencing the circle, path,
// dependent-path (pathP), fibrant, and interval groups by their registered
// hashes, so the circle group's hashes (and ch51) stay byte-stable:
//
//	circInd : (P : El Circle -> UF) -> (b : El (P base))
//	            -> (l : El (pathP (λi. P (papp Circle base base loop i)) b b))
//	            -> (x : El Circle) -> El (P x)
//
// The loop method l is a DEPENDENT path (pathP) over the line λi. P (loop@i),
// since the motive varies along the loop. circInd COMPUTES (core.CircIndInfo /
// tryCircIndIota): `circInd P b l base ~> b`, and along the loop `circInd P b l
// (papp Circle base base loop i) ~> pappP (λi. P (loop@i)) b b l i`. The dependent
// hcomp branch (comp over the motive line) is the deferred tail. No hash bump.

var circIndName = "circInd"

type circIndEntry struct {
	hash core.Hash
}

// AddCircInd registers the dependent circle eliminator against the registered
// circle, path, dependent-path, fibrant, and interval groups.
func (s *Store) AddCircInd(circle [4]core.Hash, path [2]core.Hash, pathp [3]core.Hash, fib [10]core.Hash, iv [6]core.Hash) core.Hash {
	ty := circIndType(circle, path, pathp, fib, iv)

	g := newHasher()
	g.Write([]byte{defFormatVersion, 'T'})
	th := core.HashTerm(ty)
	g.Write(th[:])
	var group core.Hash
	copy(group[:], g.Sum(nil))

	h := newHasher()
	h.Write([]byte{defFormatVersion, 't'})
	h.Write(group[:])
	writeUint(h, 0)
	var hash core.Hash
	copy(hash[:], h.Sum(nil))

	s.defs[hash] = Def{Type: ty}
	s.names[circIndName] = hash
	s.ci = &circIndEntry{hash: hash}
	return hash
}

// IsCircInd implements core.CircIndInfo.
func (s *Store) IsCircInd(h core.Hash) bool {
	return s.ci != nil && s.ci.hash == h
}

// CircIndHash implements core.CircIndInfo.
func (s *Store) CircIndHash() (core.Hash, bool) {
	if s.ci == nil {
		return core.Hash{}, false
	}
	return s.ci.hash, true
}

// CircIndName returns the surface name of the member.
func CircIndName() string { return circIndName }

func circIndType(circle [4]core.Hash, path [2]core.Hash, pathp [3]core.Hash, fib [10]core.Hash, iv [6]core.Hash) core.Tm {
	v := func(i int) core.Tm { return core.Var{Idx: i} }
	pi := func(name string, dom core.Tm, cod core.Tm) core.Tm {
		return core.Pi{Dom: dom, Cod: core.Scope{Name: name, Body: cod}}
	}
	app := func(f core.Tm, xs ...core.Tm) core.Tm {
		for _, x := range xs {
			f = core.App{Fn: f, Arg: x, Icit: core.Expl}
		}
		return f
	}
	uf := core.Tm(core.Ref{Hash: fib[0]})
	el := func(x core.Tm) core.Tm { return app(core.Ref{Hash: fib[1]}, x) }
	circleRef := core.Tm(core.Ref{Hash: circle[0]})
	baseRef := core.Tm(core.Ref{Hash: circle[1]})
	loopRef := core.Tm(core.Ref{Hash: circle[2]})
	pappRef := core.Tm(core.Ref{Hash: path[1]})
	pathPRef := core.Tm(core.Ref{Hash: pathp[0]})
	_ = iv

	// line = λi. P (papp Circle base base loop i), built under a context where P
	// is at the supplied de Bruijn index pIdx (before the λi binder).
	line := func(pIdx int) core.Tm {
		// under the λi binder, P shifts to pIdx+1 and i = v(0)
		loopAt := app(pappRef, circleRef, baseRef, baseRef, loopRef, v(0))
		return core.Lam{Body: core.Scope{Name: "i", Body: app(v(pIdx+1), loopAt)}}
	}

	// circInd : (P : El Circle -> UF) -> (b : El (P base))
	//             -> (l : El (pathP (λi. P (loop@i)) b b))
	//             -> (x : El Circle) -> El (P x)
	//   stacks: [P]; [b,P]; [l,b,P]; [x,l,b,P]
	return pi("P", pi("z", el(circleRef), uf),
		pi("b", el(app(v(0), baseRef)), // P=v(0) under [P]
			// l : pathP (λi. P (loop@i)) b b — under [b,P]: P=v(1), b=v(0)
			pi("l", el(app(pathPRef, line(1), v(0), v(0))),
				pi("x", el(circleRef), // under [l,b,P]
					el(app(v(3), v(0))))))) // result El (P x): P=v(3), x=v(0) under [x,l,b,P]
}
