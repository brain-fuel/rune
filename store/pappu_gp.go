// Code generated by goplus from pappu.gp. DO NOT EDIT.
//goplus:v v0.15.0

package store

import "goforge.dev/rune/v3/core"

// The type-level path APPLICATION builtin (§F / R-UA / A7): the `pathU` analogue of
// `papp`. A `pathU A B` is a path of fibrant CODES (a line of types); `pappU`
// applies it at an interval point to read the type at that point:
//
//	pappU : (A : UF) -> (B : UF) -> pathU A B -> I -> UF
//
// It is the eliminator `pabsU` was missing — with it the functorial action of a
// fibrant type family on a `pathU` is derivable (`apU P p := pabsU (λi. P (pappU A B
// p i))`), which lifts univalence from bare transport (`castU`) to transport in
// dependent FAMILIES. A separate group on its own hash space (mirrors `pabsU`),
// built against the fibrant and interval groups, so their hashes — and every earlier
// chapter — stay stable.
//
// pappU COMPUTES by ι (core.PappUInfo / the evaluator's tryPappUIota), exactly
// parallel to papp's β / refl / boundary:
//
//	pappU A B (pabsU line) i   ~>  line i     β: applying an abstraction
//	pappU A A (ureflU A')  i   ~>  A          reflexivity is the constant type-path
//	pappU A B  p           i0  ~>  A          boundary: at i0 a path is its left end
//	pappU A B  p           i1  ~>  B          boundary: at i1 a path is its right end
//
// No hash-format bump (no new core constructor).

var pappUNames = [1]string{"pappU"}

type pappUEntry struct {
	hashes [1]core.Hash
}

// AddPappU registers the type-level path-application group against the fibrant and
// interval groups, binding its name and returning the member hash.
func (s *Store) AddPappU(fib [10]core.Hash, iv [6]core.Hash) [1]core.Hash {
	tys := pappUTypes(fib, iv)

	g := newHasher()
	g.Write([]byte{defFormatVersion, 'V'})
	for _, ty := range tys {
		th := core.HashTerm(ty)
		g.Write(th[:])
	}
	var group core.Hash
	copy(group[:], g.Sum(nil))

	var hs [1]core.Hash
	for i := range hs {
		h := newHasher()
		h.Write([]byte{defFormatVersion, 'v'})
		h.Write(group[:])
		writeUint(h, uint64(i))
		copy(hs[i][:], h.Sum(nil))
	}
	for i, ty := range tys {
		s.defs[hs[i]] = Def{Type: ty}
		s.names[pappUNames[i]] = hs[i]
	}
	s.ppu = &pappUEntry{hashes: hs}
	return hs
}

// PappURoleOf implements core.PappUInfo.
func (s *Store) PappURoleOf(h core.Hash) core.PappURole {
	if s.ppu == nil {
		return core.PPURoleNone
	}
	if h == s.ppu.hashes[0] {
		return core.PPURoleApp
	}
	return core.PPURoleNone
}

// PappUHash implements core.PappUInfo: the member hash, for callers to construct
// type-level path applications.
func (s *Store) PappUHash() (core.Hash, bool) {
	if s.ppu == nil {
		return core.Hash{}, false
	}
	return s.ppu.hashes[0], true
}

// PappUHashes returns the registered group's hashes (and whether it exists).
func (s *Store) PappUHashes() ([1]core.Hash, bool) {
	if s.ppu == nil {
		return [1]core.Hash{}, false
	}
	return s.ppu.hashes, true
}

// PappUNames returns the surface names of the group members, in hash order.
func PappUNames() [1]string { return pappUNames }

// pappUTypes builds the one member type, referencing the fibrant group (UF =
// fib[0], pathU = fib[7]) and the interval (I = iv[0]).
func pappUTypes(fib [10]core.Hash, iv [6]core.Hash) [1]core.Tm {
	v := func(i int) core.Tm { return core.Var{Idx: i} }
	pi := func(name string, dom core.Tm, cod core.Tm) core.Tm {
		return core.Pi{Dom: dom, Cod: core.Scope{Name: name, Body: cod}}
	}
	app := func(f core.Tm, xs ...core.Tm) core.Tm {
		for _, x := range xs {
			f = core.App{Fn: f, Arg: x, Icit: core.Expl}
		}
		return f
	}
	uf := core.Tm(core.Ref{Hash: fib[0]})
	pathU := func(a, b core.Tm) core.Tm { return app(core.Ref{Hash: fib[7]}, a, b) }
	iTy := core.Tm(core.Ref{Hash: iv[0]})

	// pappU : (A : UF) -> (B : UF) -> pathU A B -> I -> UF
	pappUTy := pi("A", uf, //                          [A]: A=0
		pi("B", uf, //                                 [B,A]: B=0,A=1
			pi("p", pathU(v(1), v(0)), //              p : pathU A B
				pi("i", iTy, //                        i : I
					uf)))) //                          -> UF

	return [1]core.Tm{pappUTy}
}
