// Code generated by gpp from hash_test.gpp. DO NOT EDIT.
//gpp:v v0.11.0

package core

import "testing"

// \x -> x  and  \y -> y  are alpha-equivalent; under the locally-nameless core they
// are literally the same de Bruijn term, so they must hash equal. Scope.Name differs
// and must not affect the digest.
func TestAlphaEquivalentTermsHashEqual(t *testing.T) {
	idX := Lam{Body: Scope{Name: "x", Body: Var{Idx: 0}}}
	idY := Lam{Body: Scope{Name: "y", Body: Var{Idx: 0}}}
	if HashTerm(idX) != HashTerm(idY) {
		t.Fatalf("alpha-equivalent terms hashed differently: %s vs %s", HashTerm(idX), HashTerm(idY))
	}
}

// Distinct constructors must not collide.
func TestDistinctTermsHashDistinct(t *testing.T) {
	a := Lam{Body: Scope{Name: "x", Body: Var{Idx: 0}}} // \x -> x
	b := Lam{Body: Scope{Name: "x", Body: Univ{}}}      // \x -> U
	if HashTerm(a) == HashTerm(b) {
		t.Fatalf("distinct terms collided: %s", HashTerm(a))
	}
}

// Hashing is deterministic.
func TestHashDeterministic(t *testing.T) {
	tm := Pi{Dom: Univ{}, Cod: Scope{Name: "A", Body: Var{Idx: 0}}}
	if HashTerm(tm) != HashTerm(tm) {
		t.Fatal("hashing is not deterministic")
	}
}
