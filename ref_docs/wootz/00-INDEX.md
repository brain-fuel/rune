# Wootz research fan-out — index

Research nodes for the Wootz roadmap (`/home/brainfuel/.claude/plans/humble-humming-elephant.md`).
Each node below links to its full write-up with a one-line hook. Status is the node's
own self-assessment: **[ready-to-build]** means a concrete implement node can start;
**[needs-more-research]** means it is still gated on open questions or upstream nodes.

Cross-checked against the roadmap DAG (tracks A/X/C/B/D/E, milestones M0–M7).

**Consolidated design records** (decision-locked, grounded in landed code):
- **[RBOX-DESIGN](RBOX-DESIGN.md)** — proper-face open-box `hcomp`. Grounded findings: the ⊥ path fill already worked (no rule needed); and because a pathF hcomp's walls are themselves paths a→b, its endpoints fall out of the papp-boundary, so the **pathF hcomp rule needs no per-face dispatch and works for ANY face** (improving R-BOX.md's "single-atom only"). **AS BUILT:** `PathHash` + the pathF rule landed + ch46 (unblocks A3/A4). `fib`/dependent-`sigmaF`/`Glue` faces still stuck.
- **[GLUE-DESIGN](GLUE-DESIGN.md)** — A6/A7 build plan synthesizing R-GLUE + C-OVERLAP + R-UA against the *landed* `sigmaF`/`transpG`/paths/faces. Locks: Glue is a **separate** group (mirrors landed `sigmaF`, not an extended `FibRole`); the ready slice (group + glue/unglue + both boundaries + ⊤/⊥ degeneracies + `Equiv`) is buildable **now**; the transp/hcomp fixups stay stuck on R-BOX + `∀i.φ`. C-OVERLAP resolved to option (b). 8-step build order.
- **[NUCONS-DESIGN](NUCONS-DESIGN.md)** — **the cubical coinduction principle that CLOSED the E2 converse.** The roadmap's parked "dedicated multi-iteration research effort" is answered: the missing primitive was a **one-level final-coalgebra constructor `nuCons : (F:UF->UF) -> El (F (Nu F)) -> El (Nu F)`** (4th coind member, no hash-format bump) with β `out F (nuCons F x) ~> x` and the **coinductive η** `nuCons F y ≡ t ⟺ y ≡ out F t` (`convNuConsEta`) — the negative-type dual of inner-Σ pair+η. It makes cons-η `consStr (head s)(tail s) ≡ s` DEFINITIONAL (the single coherence every route bottomed out at), so `bisimToPathStr` (bisimilarity ⟹ path for `Str A`) closes **computing, no postulate, no dfix-observation**. Repair companions also landed: Kan-over-Nu (`out` commutes with `hcomp`) + final-coalgebra η (`unfold (out) ~> id`). **AS BUILT + GREEN:** `store/coind.go` `NRoleNuCons`, `core/eval.go` `tryCoindIota`/`tryNuConsEta`/`tryUnfoldEta`, `core/conv.go` `convNuConsEta`; ch69 `bisimToPathStr`, consumed by E3 trace-equality (ch70). **`isEquiv glue` is demoted to an OPTIONAL alternate-bridge residue, not E2-gating.**

---

## By track

### Track A — cubical inner stratum (telos 1)

- **[R-FILL](R-FILL.md)** [ready-to-build] — the keystone: ship CCHM generalized transport `transpG A φ a0` as a 4th Kan-group member, derive fwd/bwd `transpFill` value-level over the De Morgan algebra; `comp = transpFill ▸ hcomp`; non-regular CCHM gated on X2.
- **[R-BOX](R-BOX.md)** [ready-to-build] — genuine open-box `hcomp` filling by recursion on the head former (per-former ι-rules in `tryHcomp`); piF/single-atom-pathF buildable now, multi-disjunct faces wait on C-OVERLAP.
- **[R-SIGMA](R-SIGMA.md)** [ready-to-build] — `sigmaF`/`pairF`/`fstF`/`sndF` as a bodiless content-addressed builtin group, `El (sigmaF…)` stays neutral; transpFill-free Kan now, fully-dependent fill stuck on R-FILL.
- **[R-GLUE](R-GLUE.md)** [landing — G1] — `Glue`/`glue`/`unglue` shipped (`store/glue.go`); hcomp/comp-over-Glue + the `glue`-intro/φ-constant transp-over-Glue (`transpGlueIntro`) **landed** (ch50). Residue: the **general** transp-over-Glue arm (neutral input / φ varying in i, `core/eval.go:2913-2917`) — wire the landed `RestrictIv`+`extend` through full `compGlue`. R-FILL/R-BOX/`∀i.φ`/C-OVERLAP all resolved.
- **[R-UA](R-UA.md)** [DONE — postulate RETIRED] — univalence is fully DERIVED. `ua` is no longer a builtin or a postulate: it is an ambient prelude definition (`internal/session/uaprelude.rune`, `ua A B f g s t := uaGlue …`, a Glue line), and `castU` along it computes the forward map through the genuine transp-over-Glue arm. DELETED: the postulated `ua` head (fib group 11→10 members), the baked-in `castU`-over-`ua` fiat rule (`core/eval.go`), and `FRoleUa`. The derived-univalence library (`eqToPathF`, `triangleCoh`, `isoToEquiv`, `equivFromIso`, `idEquivF`, `uaGlue`, `ua`) is elaborated once and injected ambiently into every session, so ch10/fib_test use `ua` unchanged through the derived route. `flipped` still runs to `false` (B5 erase-the-normal-form). Full suite green. Remaining §F: path induction OVER a ua-path.
- **[R-HIT](R-HIT.md)** [ready-to-build] — inner HITs as a per-declaration builtin group (fibrant former + neutral point/path ctors + generated eliminator + formal `hcomp` ctor); dim-≤1 monomorphic slice (Circle, Quotient, constant Susp) lands now.

(R-UFH — fibrant universe hierarchy UF_i — has no node in this fan-out; the roadmap marks it "only if needed" and no implement node depends on a delivered R-UFH yet.)

### Track X — soundness guard (gates every A milestone)

- **[R-FRAME](r-frame.md)** [ready-to-build] — keep one proof-cache key `hash(h,‖U‖)` but close U over inner cubical reduction; fix the soundness gap by routing every constancy probe through a force-before-probe logging gateway, plus a self-retiring imprecise fallback for Glue/ua.

### Track C — core maturation (telos-3 enabler)

- **[R-SUM](R-SUM.md)** [ready-to-build] — add a binary negative Sigma (`Sig/Pair/Fst/Snd`) to the OUTER core with definitional eta (the single Thompson-sensitive `core/conv.go` edit); n-ary records are sugar; resolves C-ABS toward typeclass-records + implicit search with an explicit-dictionary floor.
- **[R-NUM](R-NUM.md)** [ready-to-build] — node **C7**: a compressed core numeral `NatLit{*big.Int}` (definitionally `succ^n zero`, so D1's induction proofs transfer) + an opt-in kernel acceleration table (the user's own `add`/`mul`/… get a literal-only bigint ι-rule, bridge = `refl`). Kills the 4096 unary cap (`numeral.go:35`) AND the `mul 4096 4096` blowup (O(a·b) succ nodes today); bare numerals lower to `NatLit` by default; pretty folds back to a digit. Third qualifying kernel change (hash bump 0x05→0x06); **retires `builtin bin`** (Rule 5 — `NatLit`+accel subsume it).
- **[R-EFFECT](R-EFFECT.md)** [ready-to-build] — monadic-IO base as a contained builtin group (`World/IO/pureIO/bindIO`), `IO A` erases the world token via QTT; one new codegen node `IForeign{Op,Args}`; algebraic effects deferred to a library.
- **[R-PART](R-PART.md)** [ready-to-build] — three-tier totality lattice (Idris total/covering/partial) onto Rune's containment; one new kernel firewall makes a `partial` head permanently neutral so NbE never diverges; Bove-Capretta `Acc`/`wfRec` is the promotion bridge; resolves C-PARTLAW.
- **[R-COIND](R-COIND.md)** [C5a + C5b + ∀κ/force LANDED] — coinduction as two contained builtin groups: C5a final-coalgebra `Nu/out/unfold` (landed), and **C5b TCTT guarded-recursion `Later`/`next`/`dfix` (store/guard.go, tag M/m, own hash space) — LANDED.** Heads permanently neutral (no eval divergence); the guarded-fixpoint equation `dfix A f ≡ f (next A (dfix A f))` is a BOUNDED one-step CONVERSION (core/conv.go `convDfix` — the inner dfix is matched by spine comparison, never re-unfolded, so it terminates); productivity is by typing (the ▹ guard), no syntactic checker, no sizes. ch62 pins the unfolding + a degenerate fixpoint reducing. Unblocked by M2 (paths carry computational content). **+ `lmap` (the ▹ functor map, 4th member): `lmap A B g (next A x) ~> next B (g x)` by eval ι (tryGuardIota) — ▹ is now a genuine FUNCTOR (lifts any function under the later), the substrate guarded corecursion needs; ch62 functor pins.** **+ BISIMILARITY-AS-PATH, forward direction (ch66): for coinductive streams `Str A = Nu (λX. A×X)`, a `pathF (Str A) s t` carries to `pathF` between every observation — `headPath`/`tailPath` (apP of head/tail along the path) — so a path ENTAILS pointwise bisimilarity (equal heads, path between tails). The E2 correctness currency on the cubical stratum.** **+ `lap` (the applicative ⊛, 5th member): `lap A B (next _ f) (next A x) ~> next B (f x)` by eval ι (tryGuardLapIota) — with `next` (pure) and `lap`, ▹ is now a full APPLICATIVE functor (next/lmap/lap); ch62 pin.** **+ ▹ is a VERIFIED LAWFUL applicative (ch62 `lmapAsLap` = `lmap g = lap (next g)`, `lapInterchange` = the applicative interchange law; both refl on `next` intros) — the lawful algebra E2's guarded corecursion is built from.** **+ CLOCK QUANTIFICATION `∀κ` + `force` LANDED (CLOCKS-DESIGN steps 1–3; ch68, store/guard.go now CLOCK-EXPLICIT, 8 members):** the guard group is re-indexed so `Later : Clock -> UF -> UF` (▹κ on a NAMED clock), `Clock : U` is a pretype (no eliminator, like `I`), `k0 : Clock` a constant, and `∀κ. A` is an ordinary Pi over `Clock` (no new former). The keystone `force : (A:UF) -> ((k:Clock) -> El (Later k A)) -> ((k:Clock) -> El A)` is the SOLE `▹`-elimination, computing by the clock-β / force-next ι `force A (λκ. next κ A x) ~> λκ. x` (tryForceIota: fires only on a `next` on the BOUND clock with a clock-CLOSED payload, neutral otherwise — the C4 firewall holds, the normalizer cannot diverge). SOUNDNESS by clock-indexing: the inconsistent `Later A -> A` is UNTYPABLE — a `λκ. next c A x` delayed off the quantified clock fails to elaborate (TestForceWrongClockRejected); force on a neutral stays stuck (TestForceStaysStuckOnNeutral). ch68 pins forceNext / escapeId / forceLmap (all refl); ch62 re-elaborates clock-indexed (digest shift in its own hash space, no golden test). No hash-format bump. **+ GUARDED-RECURSIVE TYPES `gfix` LANDED (9th guard member; ch69):** `gfix : (k:Clock) -> (UF -> UF) -> UF`, the TYPE-level dual of `dfix` (which is value-level for a fixed code) — the primitive needed to even STATE the guarded stream `Str^g κ A = gfix κ (λX. A × ▹κ X)` and so the bisimilarity relation. Its head is neutral in eval (no divergence); the equation `gfix k F ≡ F (gfix k F)` is a BOUNDED conversion (core/conv.go convGfix) with a PROGRESS GUARD — it unfolds once and only when F makes head progress, so an unguarded `λX. X` cannot loop the checker (TestGfixUnguardedTerminates). ch69 builds the guarded stream + `consG`/`headG`/`tailG` (all type-check through the convGfix fold AND compute: `headG (consG h t) ~> h`, refl) + `repeatG` (a `dfix`-driven producer, its guarded-fixpoint unfolding refl-pinned) **+ fmapF-OVER-LATER (CLOCKS-DESIGN step 5): `sfmap` (the stream-functor carrier map) lifts under ▹κ via `lmap` and COMPUTES on a `next` of a `pairF` — `lmap κ (sfmap g) (next κ (h,x)) ~> next κ (h, g x)`, refl.** **+ DEPENDENT FORCE `forceD` LANDED (CLOCK-IRRELEVANCE-DESIGN step 1; 10th guard member; ch69):** `forceD : (A:Clock->UF) -> ((k:Clock) -> El (Later k (A k))) -> ((k:Clock) -> El (A k))`, the clock-quantified ▹-elimination for a clock-VARYING payload type (plain `force` needs a clock-CLOSED payload, but a guarded stream's tail `▹κ (gStr κ A)` is clock-varying). ι `forceD A (λκ. next κ (A κ) x) ~> λκ. x` (tryForceDIota), payload x MAY mention κ; SOUND by clock-INERTNESS (`Clock` has no eliminator → a payload can mention κ but never inspect it → g is uniform, the sentinel probe is representative; the result re-applies g at the bound clock and strips the `next`). Stays stuck on a neutral (TestForceDStaysStuckOnNeutral). ch69 lands forceDNext + the GLOBAL stream observations `gheadG`/`gtailG` (the clock-quantified guarded stream, the bridge to `Str A`) + `gtailCons` — `forceD` cashing a guarded stream's delayed tail into the global rest, the E2-converse OBSERVATION on the genuinely clock-varying `gStr` type (refl). **+ INDEXED guarded fixpoint `gfixF` LANDED (CLOCK-IRRELEVANCE-DESIGN step 3, the `Bisim` former; 11th guard member; ch69):** `gfixF : (k:Clock) -> (D:UF) -> ((El D -> UF) -> (El D -> UF)) -> (El D -> UF)` — `gfix` recurses a single code, `gfixF` recurses a FAMILY indexed by D so the recursive occurrence can sit at a DIFFERENT index (what bisimilarity needs — it recurses on the stream TAILS, not the same pair). Equation `gfixF k D Φ d ≡ Φ (gfixF k D Φ) d`, a bounded conversion on the APPLIED (4-arg) fixpoint (core/conv.go convGfixF) with a progress guard (an unguarded `Φ = λR. R` cannot loop — TestGfixFUnguardedTerminates). ch69 pins `gfixFUnfold` (the equation for any Φ, refl) + `gfixFStep` (Φ recursing at a STEPPED index `gstep d` under ▹κ — the indexed guarded corecursion, refl). **+ UNIVERSE-LEVEL ▹κ APPLICATION `laterApp` LANDED (12th guard member; ch69):** `laterApp : (k:Clock) -> (A:UF) -> (El A -> UF) -> El (Later k A) -> UF`, the companion to `lmap` for a UF-VALUED family — `lmap` lifts an `El A -> El B` function under ▹κ, but bisimilarity's recursive occurrence is a RELATION (UF-valued), so it needs `laterApp k A f (next k A x) ~> Later k (f x)` (tryLaterAppIota) to sit a UF-valued recursive call under ▹κ. ch69 pins `laterAppNext` (refl) + **`gRelStep`** — the BISIMILARITY SHAPE abstractly: a `gfixF` relation `Φ R e = (P e) × laterApp k D R (step e)` whose UF-valued recursive occurrence is at a STEPPED, DELAYED index `step e : ▹κ D` and under ▹κ via `laterApp`; it unfolds by refl (bisimilarity over streams instantiates D = stream pair, P = head-path, step = delayed tail pair). **+ the CONCRETE stream `gBisim` LANDED (ch69):** bisimilarity of guarded streams built with `gfixF` + `laterApp` — index `Dpair = gStr × gStr` (a pair of guarded streams), `gBisim d = (headG (fst d) = headG (snd d)) × ▹κ (gBisim (tail-pair d))` where the head-equality is a `pathF`, the recursive occurrence sits under ▹κ via `laterApp`, and the `step` (delayed tail pair `gstepPair`) zips the two ▹κ-tails into a `▹κ`-pair via `lap`∘`lmap`. `gBisimUnfold` proves the coinductive characterisation `gBisim d ≡ (head=head) × ▹κ gBisim(tails)` by refl. **+ DEPENDENT guarded application `lapD` LANDED (13th guard member; ch69):** `lapD : (k:Clock) -> (A:UF) -> (B:El A -> UF) -> El (Later k (piF A B)) -> (la : El (Later k A)) -> El (laterApp k A B la)` — `lap` applies a delayed NON-dependent function, `lapD` a delayed DEPENDENT one, its result TYPE given by `laterApp` (the reason laterApp came first); ι `lapD k A B (next k (piF A B) f) (next k A x) ~> next k (B x) (f x)` (tryLapDIota). ch69 `lapDNext` pins it (refl). This is the combinator the path-assembly recursion needs to apply the delayed recursive path-builder to the delayed tail index. **+ the COINDUCTIVE DESTRUCTORS `bisimHead`/`bisimTail` LANDED (ch69):** since `gBisim d` unfolds to `(head-path) × ▹κ (tail-bisim)`, an element projects to the head-equality `pathF` (`bisimHead` = fstF) and the delayed tail-bisimilarity (`bisimTail` = sndF) — the converse's observation layer. **KEY FINDING (this iteration): the path-assembly endpoint coherence is NOT definitional.** A `pabs (gStr) (λi. consG (papp hp i)(lmap (λq. papp q i) tailpath))` has boundary `consG (headG s)(lmap (λq.papp q i0) tailpath)`, but `lmap (λq.…) tailpath` stays a NEUTRAL lmap (tailpath is delayed/neutral), so it does NOT reduce to `tailG s` — hence `consG (headG s)(…) ≢ s` by inner-Σ η. So the converse is a CUBICAL construction: the path must be built with approximate endpoints then REPAIRED by Kan composition (`comp`/`hcomp` over `gStr`) to land its endpoints on `s`/`t` — not bare guarded corecursion. **+ the ENDPOINT-REPAIR ENABLER LANDED (core/eval.go; clock_test): `hcomp` over a `gfix`-typed value now reduces.** A guarded stream `gStr k A = gfix k F` has a gfix-NEUTRAL head, which the structural Kan formers (piF/pathF/sigmaF) do not match — so Kan ops over it were stuck. Added `unfoldGfixType` + a tryHcomp case: unfold the gfix TYPE one step (`gfix k F ≡ F (gfix k F)`, exposing its `sigmaF`) and recurse, so the landed structural-Σ Kan rule (A5a/A5b) fires — `hcomp (gStr) φ u u0 ~> pairF …` (TestHcompOverGfixUnfolds). Sound (El equal) + bounded (the body's former is sigmaF, fires once). **+ the repair VERIFIED componentwise (ch69 `hcompHeadG`/`hcompTailG`, refl): `headG (hcomp gStr φ u u0) ≡ hcomp A φ (heads)` and `tailG (…) ≡ hcomp (Later k gStr) φ (tails)`** — hcomp over a guarded stream acts independently on head and tail, so the endpoint-repair keeps observations coherent. NEXT-OBSTACLE FINDING (in the path-assembly proof): any corecursive proof over `gBisim` must fill the `laterApp k Dpair gBisim (gstepPair d)`-typed tail SLOT, but the natural recursive call (via `lapD` over `gStr`) produces a differently-shaped `laterApp k gStr (λs'. gBisim …) (tailG s)` — the two laterApp neutrals do not definitionally align, so the recursion needs a SLOT-COHERENCE (the `gstepPair`/recursion bridge) to retype. **E2 CONVERSE NOW CLOSED — see [NUCONS-DESIGN](NUCONS-DESIGN.md).** The parked "dedicated cubical-coinduction research effort" is answered by the one-level final-coalgebra constructor `nuCons` + its coinductive η (`convNuConsEta`), which makes cons-η definitional and closes `bisimToPathStr` (bisimilarity ⟹ path for `Str A`) **globally, computing, no postulate** (ch69), consumed by E3 trace-equality (ch70). The per-clock `Bisim ≃ pathF` guarded assembly + the `Str ≃ ∀κ. gStr` bridge (`isEquiv glue`) are an OPTIONAL ALTERNATE route, no longer E2-gating; the clock-irrelevance axiom and `Λκ` freshness pass go with it. Sized types rejected.

### Track B — portable codegen + FFI (telos 2)

- **[R-IR](R-IR.md)** [ready-to-build] — grow the throwaway shadow IR (never the core): sealed nodes `ICon/ICase/ILit/IPrim/IForeign/IInner`, lower each eliminator once via `LowerElim`, eval/apply curried ABI in a per-backend Repr table; BEAM-first via Core Erlang; structural `IInner` deploy ban replaces the name-walk.
- **[R-ERASE2](R-ERASE2.md)** [ready-to-build] — erase inner constructs against the kernel NORMAL FORM in three classes; only genuinely-stuck Class-3 transports emit `IInner`, so the deploy ban fires precisely; lifts the inner-taint ban (this IS B5).
- **[R-FFI](R-FFI.md)** [ready-to-build] — a foreign function is a bodiless neutral axiom whose TYPE is its Rune contract (discharge/assume/guard tiers); assumed axioms tracked in a write-only "assumed" set A giving a backward-compatible cache key + free `rune assumptions`; C-ABI via a CRepr table.
- **[R-NATIVE](R-NATIVE.md)** [conversion LANDED; GC runtime needs-more-research] — the native (LLVM/Cranelift) half of M4: closure conversion / lambda lifting over the erased IR (`codegen/closure.go` `ClosureConvert(Program) -> ClosureProgram`), making the implicit host closure EXPLICIT — `MkClosure{code-ptr, captured-env}` + `AppClosure` + lifted closed `CodeBlock`s — the prerequisite for any backend with no host closures/GC. Semantics-preservation proven by a dual evaluator (original `Ir` vs converted `CIr`) on the conformance corpus; closed-block + free-var tests; opt-in, no hash bump, no shipped-output change. Specifies the native ABI (uniform tagged-word value rep, eval/apply over `{code,env}`, a semispace closure heap). Research-deferred: the GC'd closure runtime + the first Cranelift/LLVM emitter over the converted IR.

### Track D — tiered-verified standard library (telos 3)

- **[R-INTEROP](R-INTEROP.md)** [ready-to-build] — a NumPy/BLAS array is `Array dt sh`, a shape/dtype-indexed opaque foreign handle: shape conformance discharged by type-checking, dtype/finiteness guarded at the boundary, math assumed + logged; one new `CArray` CRepr; worked artifact `fit.rune`.
- **[R-OTP](R-OTP.md)** [needs-more-research] — OTP concurrency as a three-layer tiered-verified library over R-CALC's `Proc` + R-EFFECT's IO: R0 six `IForeign` primitives + `Pid` former, R1 typed actor loop, R2 gen_server/Supervisor as record-of-callbacks with theorems against the `Step` LTS; liveness waits on R-COIND.
- **[R-HOT](R-HOT.md)** [ready-to-build in layers] — verified hot reload / live upgrade (node D7). A four-tier change classification (0 identical-hash no-op/rename, 1 definitional relink = "link something new", 2 univalence transport = automatic+proven migration, 3 breaking = gated total migration) + migration synthesis over the state's Σ-telescope + the `UPGRADE` `Step` constructor with invariant-carry + the BEAM `code_change` projection (content-hash as `-vsn`, one `IForeign` `primUpgrade`, no IR node, no hash bump). Tier-2 is the deployed payoff of A7 (`castU (uaGlue …) ~> f x` deploys via B5). The dev-loop `:reload` half is ready NOW (content-addressing + a dependency walk); live-process upgrade is gated on D5+B3.

### Track E — distributed process-calculus algebra (telos 4)

- **[R-CALC](R-CALC.md)** [needs-more-research] — a shallow-embedded reflexive async pi/actor calculus with ZERO outer-core constructors: `Proc` is data-by-eliminators, `Step` is a Rune relation, real failure is more `Step` constructors; bisimulation gated on R-COIND; projection to OTP via `IForeign`.
- **[R-PROTO](R-PROTO.md)** [needs-more-research] — a three-layer refinement stack (spec → protocol LTS → projected actors, each a simulation) as a library over R-CALC; CRDT G-Counter is the inductive M0 safety demonstrator, single-decree consensus the harder M7 artifact; liveness + adequacy gated on R-COIND.

### Clarify node (own write-up)

- **[C-OVERLAP](C-OVERLAP.md)** [ready-to-build] — resolve C-OVERLAP via option (b): leave `core/conv.go` untouched, discharge system overlap-agreement as a checker-side `sys` smart-constructor obligation (branches are proof-opaque so conversion succeeds); reject the global irrelevant-Prop rule.

(The other clarify decisions are answered *inside* node docs: C-REG in R-FILL, C-ABS in R-SUM, C-PARTLAW in R-PART, C-INFRA in R-CALC/R-PROTO.)

---

## Implement nodes now UNBLOCKED

A DAG implement node appears here when at least one research dep is **ready-to-build**
and the node's own doc declares a buildable increment. Each entry states the DAG deps
and what the *remainder* of the node still waits on.

### Track A
- **A1** (varying-domain piF transp/comp) ⇐ R-FILL — **FULLY unblocked** by R-FILL's `transpFillB`. Still waits on: the `transpG` stuck-form decision being settled first (recommended: add it as a Kan member); build alongside X1.
- **A2** (proper-face fib/piF fills) ⇐ R-FILL, R-BOX, C-REG — **partially unblocked**: R-BOX delivers piF-push, fib-⊥, and the proper single-atom pathF fill now; R-FILL gives the comp seam; C-REG is resolved inside R-FILL. Still waits on: C-OVERLAP/A8 for proper multi-branch faces.
- **A5** (sigmaF group + Kan over sigmaF) ⇐ R-SIGMA, A2 — **A5a unblocked** (group + computing projections + neutral El + transpFill-free/constant-line Kan). Still waits on: A5b (fully-dependent componentwise transp/hcomp/comp) blocked on R-FILL; A2 for proper faces.
- **A9** (inner HIT kit + cubical computation) ⇐ R-HIT, A6 — **LANDED (circle slice): the circle is a genuine Kan HIT.** store/hit.go (group tag H/h): Circle/base/loop/circElim; recursor computes by ι on base (`~> b`) and along loop (`~> papp P b b l i`), and COMMUTES with hcomp (CHM hcomp-as-constructor: `circElim … (hcomp Circle φ u u0) ~> hcomp P φ (λi h. circElim … (u i h)) (circElim … u0)`). Boundary + constant-line transport are free (path group + regularity). loop ≠ refl (distinct neutral, ua≠ureflU coexistence). ch51_circle.rune; commits e07f1b0 + 9fb8a42, no hash bump. **The full dim-≤1 ready slice now lands: + Suspension (store/susp.go, tag J/j — Susp/north/south/merid/suspElim, the S^n machine) and the fibrant Quotient (store/quotient.go, tag L/l — Quotient/qinc/qrel/quotElim, the path-respecting EFFECTIVE quotient strict Quot can't give), both parameterised, recursors computing on points + path ctor + commuting with hcomp; ch53_suspension.rune + ch54_fibquotient.rune; commit bd269f2.** **+ dependent paths (pathP, store/pathp.go tag R/r) and the circle's DEPENDENT eliminator (circInd, store/circind.go tag T/t — the induction principle, loop method a pathP over λi.P(loop@i)); ch55_circind.rune; commit 62e1c50.** **+ induction for Susp/Quotient (suspInd store/suspind.go tag A/a; quotInd store/quotind.go tag B/b) — every HIT now has recursion AND induction, the path-ctor method a pathP over the motive along the generator; ch56_hitinduction.rune; commit 819e027.** **+ induction TOTALITY (commit ec0b10b): the dependent hcomp branches for circInd/suspInd/quotInd (comp over the motive line λi. P (hfill (HIT) φ u u0 i)) — every HIT eliminator, recursor AND dependent, is now total on all generators (point ctors, path ctor, hcomp cell).** **+ varying-parameter transport (R-FILL): point ctors re-index (commit 7732c59), and the MERIDIAN FILLER — transport of the path constructors (suspension merid, quotient qrel) — computes (commit 083308b, RFILL-MERIDIAN-DESIGN.md): transp (λi. Susp (Afam i)) (merid a @ k) ~> merid (Afam i1) (transp a) @ k; transp over Quotient of qrel transports both carriers + the witness along the relation line. No comp correction (suspension/quotient endpoints reconcile via the qinc/pole point rule). varyparam_test.go.** **+ dim-2 SET-TRUNCATION (commit 87c15bb): Trunc0/inc/squash/trunc0Rec (store/trunc.go tag X/x) — the first dim-2 HIT (squash is a path-between-paths); statable, recursor computes on inc (trunc0Rec … (inc a) ~> f a); ch57_settrunc.rune.** Deferred (honest-stuck, transport-correctness load-bearing): the formal hcomp-cell varying transport (transp-of-hcomp = comp) and the general CHM comp-correction (recursive path-ctor endpoints); the dim-2 INTERIOR reduction (recursor along the squash square).

Special cases (research dep is needs-more-research but a real increment is ready):
- **A6** (Glue group + Kan-over-Glue) ⇐ R-GLUE, A5, A2 — former + glue/unglue + both boundaries + ⊤-degeneracies are **ready-to-build once A5/R-SIGMA lands** to supply Equiv. The full Kan-over-Glue fixups stay blocked on R-FILL + R-BOX + C-OVERLAP (shipped as auto-completing skeletons).
- **A7** (ua-as-Glue; J/castU over ua) ⇐ R-UA, A6, A4 — **DONE — postulate retired, J-over-ua computes.** Derived `ua := uaGlue` is ambient (prelude); the postulated head + fiat rule + `FRoleUa` are deleted (fib 11→10). `castU` is now honest (R-UA Decision 2): `castU A B p x ~> transp (λi. pappU A B p i) ⊥ x` for ANY path (ureflU/pabsU fast paths; a neutral path reduces to a stuck `transp`), so `transportF` in the identity family `= castU` definitionally (`transportFId`, refl). **Path induction OVER a ua-path computes:** `pathUJ` (ch58, the dependent eliminator for `pathU`, DERIVED from `transpG`+`pappU`+`pabsU`) reduces on `ureflU` (`pathUJRefl`) and transports the motive along a genuine ua-path to a value (`pathUJOverUaComputes` — over Bool's `not` ua-path → `false`, refl). Enabled by type-path η (`convPathUEta`). No ua-specific J rule, no postulate. The whole A7/M2 telos-1 line is closed.

(A3 transp/comp-over-pathF ⇐ A1,A2 and A4 pathJ-via-comp ⇐ A3 follow the A1/A2 chain — their base compositions are R-BOX's pathF hcomp, buildable as A1/A2 land, but full forms still need R-FILL + overlap. A8 ⇐ C-OVERLAP, see Track C section below. A10 ⇐ R-UFH has no research node.)

### Track X
- **X1** (two-layer closure key) ⇐ R-FRAME, A1 — **VERIFIED for cubical reduction (commit 1d096aa).** The proof-cache Frame Lemma's closure key (defHash,‖U‖) captures INNER unfolding: cubical/HIT ι-rules log the user-def bodies they reach through the same Force/Unfold seam, and the rigid bodiless builtins are correctly excluded. No production change needed (the seam was already exact); pinned by frame_cubical_test.go (inner dep captured, rigid builtins excluded, frame-invariant, certificate reusable). The soundness guard gating every A-milestone, now closed for the cubical ι-rules (HIT/Glue/path/Kan).
- **X2** (cubical property/mutation tests) ⇐ each A node — incremental; consumes X1's `runConv`-with-U to attribute frame-vs-canonicity failures and is the C-REG canonicity gate named by R-FILL. Still waits on: each A node to test against.

### Track C
- **C1** (Sigma + dependent records) ⇐ R-SUM — **FULLY unblocked**: core Sigma, beta/eta/proj conversion, hash bump, value-level Sigma-Eq rule, record sugar. (Eq-U type-former telescope lands with its first listing consumer.)
- **C2** (ad-hoc polymorphism layer) ⇐ C-ABS, C1 — **LANDED** (basic non-overlapping typeclass-record + implicit search + explicit-dictionary floor, zero core; ch31).
- **C2b** (recursive parametric instance search) ⇐ C2 — **unblocked, zero core, elaborator-only.** Resolve a constrained instance from its parts (`Ord A => Ord (List A)`, `Eq A => Eq (Pair A B)`, …) by depth-first sub-goal resolution building the dictionary bottom-up. Forcing consumer **D2** (sorting/`Map`/`Set` over compound types). **★ REWRITE MANDATE (Rule 5):** once C2b lands, **anything that would benefit from a rewrite to use parametric resolution OUGHT to get one** — every hand-written instance the search can now DERIVE, and every shape contorted to dodge the old non-recursive search, is superseded → delete/rewrite to the derived form, take the hash event, do not hoard the redundant. This is a sweep over the existing corpus + stdlib, not just a forward-only rule. **Overlapping/priority resolution stays PARKED** — no consumer; the first-instance-wins + ambiguity-error + explicit-dictionary floor already unblocks every program; promote to a node only when a real listing needs genuine overlap (the coherence/specificity policy is the cost not worth paying speculatively).
- **C3** (effects/IO layer) ⇐ R-EFFECT — **unblocked**: erased-world monadic-IO base + IForeign IR node + JS backend. Still waits on: the agreed initial op vocabulary (putStr/getLine/readFile/timeNanos) + the seq-for-IO surface desugar.
- **C4** (partiality/general-recursion) ⇐ R-PART, C-PARTLAW — **unblocked** (C-PARTLAW resolved in R-PART): the `partial`/firewall mechanism, Delay/runFor + Acc/wfRec libraries, the all-partial SCC resolver. Still waits on: a taint-strictness decision (recommend strict binary) + a one-line cache-keying confirmation.
- **C5** (coinductive types / guarded recursion) ⇐ R-COIND — **C5a LANDED** (Nu/out/unfold) **and C5b LANDED** (the guarded modality `Later`/`next`/`dfix`/`lmap`/`lap`, store/guard.go; dfix unfolds once via the bounded `convDfix`, no eval divergence; ch62) **+ CLOCKS LANDED** (`Clock`/`k0`/clock-indexed `Later`/`force`, ch68 — `force` is the sole sound ▹-elimination, computing by the force-next ι, the inconsistent `Later A -> A` made untypable by clock-indexing). The dfix trigger-discipline is resolved as a one-step conversion (not an eval ι). **+ THE CONVERSE bisimilar⟹path is now CLOSED** (global route via the one-level final-coalgebra constructor `nuCons` + coinductive η — [NUCONS-DESIGN](NUCONS-DESIGN.md); ch69 `bisimToPathStr`, computing, no postulate, consumed by E3). So the E2 consumer's correctness currency is delivered. Still waits on: R-ERASE2/B-track for an erased runtime meaning of guarded values; the per-clock `Str ≃ ∀κ. gStr` bridge (`isEquiv glue`) is optional/alternate, not gating.
- **C6** (module/namespace/package surface) ⇐ C1 — **unblocked** to represent modules as Sigma-values (per R-SUM). Still waits on: C1; the namespace surface is additional non-core work.
- **C7** (compressed numerals + kernel arithmetic) ⇐ R-NUM — **unblocked** (depends only on the existing `builtin nat`): the `NatLit{*big.Int}` core node (eval/conv/hash/quote, hash bump 0x06), default-lowering to `NatLit` + 4096-cap deletion, the opt-in accel table (add/mul/monus/divmod/eq/le/pow, literal-only ι, bridge=refl), pretty digit-folding, and the `builtin bin` retirement (Decision 5). **Accelerates D1** (closed numeric proof obligations run in bigint ops, laws transfer refl) and **templates D3** (Int/Rat/Float reuse the literal+accel pattern). The one metatheory-review event is the accel table's agreement with the unfolding (differential + mutation tested). Independent of A/B/E — buildable now.

### Track B
- **B1** (portable erased IR + runtime ABI) ⇐ R-IR — **delivered** (R-IR is the design: IR nodes + ABI + LowerElim).
- **B2** (refactor Backend to IR, JS=ref) ⇐ B1 — **KEYSTONE SLICE LANDED (JS=ref): the eliminator is LOWERED ONCE into the shared IR.** New IR nodes `IField` (project a constructor arg by index) + `ICase`/`ICaseArm` (tag dispatch, arms share the enclosing de Bruijn context — no field binders); `codegen.LowerElim(DataSpec)` lowers an eliminator to ordinary curried-lambda + ICase IR with IField projections and a by-name recursive call for the induction hypotheses. The JS backend now emits each eliminator as a plain definition `const DElim = <LowerElim>` and renders ICase/IField — **its hand-written `emitElim` is DELETED** (the R-IR "deletes emitElim from every backend" payoff, proven on JS). Gate met: byte/observationally-identical JS on the full listings + codegen corpus (TestEmitElimLoweredJS pins the switch/`.args[` shape + the recursive fold). **COMPLETE across ALL backends:** py (chained-conditional ICase), go (IIFE switch), and rust (`_tag_of`/`_field` helpers + match) now all render the lowered eliminator — `emitElimPy`/`emitElimGo`/`emitElimRust` ALL DELETED. Pin TestLowerElimRecursiveAllBackends (a non-nat recursive ListElim folds identically on js/py/go/rust). The eliminator is lowered ONCE; no backend carries eliminator-specific code. B2 done.
- **B3** (BEAM to genuine working FIRST) ⇐ B2 — **LANDED: a BEAM backend that genuinely runs.** `codegen/beam.go` emits an Erlang escript over the SAME erased IR — values are Erlang funs / integers (nat) / the `unit` atom / `{pair,…}` / `{c,Tag,Name,Args}` ctor tuples; application via an `ap/2` shim; the eliminator rendered straight from the shared `LowerElim` (ICase → an Erlang `case` on the tag, IField → `lists:nth`/`element`) — so the BEAM backend carries NO eliminator-specific code (the B2 keystone is what made B3 a small plugin). `$show`, nat fast-path, quotient carrier, and the IO monad all ship; `foreign` axioms erase to host accessors (`ff_<name>/0`). Erlang's order-independent top-level functions sidestep init cycles. Registered (`codegen.Beam`, target `erl`, aliases beam/erlang/escript) + CLI `rune run --target erl` via an escript run harness. Cross-backend conformance now includes BEAM: the full corpus (ch04/06/32/39/43 — data/nat/quotient/Σ/partial/IO) observes byte-identical stdout on js/py/go/rust/**erl**. **FULL feature parity reached:** foreign axioms link + run on BEAM (`ff_hostId/0`, TestForeignConformance/erl) and side-effecting IO runs (`io:format` host effect under the IO monad, TestIOEffectConformance/erl) — all five backends prove pure + FFI + effects. B3 "genuinely working FIRST" DONE. Remaining B-track: native backends (LLVM/Cranelift/WASM, research-gated on closure-heap+GC); OTP is D5.
- **B4** (C-ABI FFI + contract checking) ⇐ R-FFI, B2 — **unblocked** for non-dependent contracts + assume/guard tiers + assumption-tracking + scalar/String/Ptr marshalling on JS. **PORTABLE-FFI SLICE LANDED:** the `IForeign` IR node ships (codegen/ir.go) — a `foreign` axiom erases to a uniform zero-arg host accessor `<name>()` on EVERY backend (js/py/go/rust), so one host impl links identically across all four (harness/ffi_conformance_test.go); the `_d` def-thunk convention no longer leaks into the go/rust FFI symbol. **ASSUME-TIER CONTRACTS LANDED:** a `foreign` axiom carries a PRECONDITION as a 0-quantity proof argument — `foreign guardedNat : (d : Nat) -> (0 pf : Eq Bool (isZero d) false) -> Nat` — checked at compile time, ERASED at runtime (proof = the unit token; no Eq/refl leaks the shadow), run identically on all 5 backends (ch60, TestFFIContractRunsAndErases); an undischarged precondition fails to elaborate (TestFFIContractEnforced). Falls out of dependent-foreign + QTT, no new machinery. **GUARANTEE-TIER + COMPOSITION LANDED:** a `foreign` can also RETURN a refined value — `trustedNonzero : (n : Nat) -> Sig Nat (λr. Eq Bool (isZero r) false)` pairs its result with a host-guaranteed proof; that postcondition COMPOSES, discharging another foreign's precondition (`chained = guardedNat (Fst …) (Snd …)`, ch61) — both proofs erase (the guarantee lands in a 0-quantity slot), the value threads through, runs on all 5 backends (TestFFIPostconditionComposes). The assume/guarantee handshake on Σ(C1)+QTT. Still waits on: scalar/String/Ptr marshalling, R-SUM refinement-post sugar, D1 guard decidability.
- **B5** (erased meaning of inner paths/Kan; lift the ban) ⇐ R-ERASE2, A7, B2 — **SLICE LANDED (commit 189660d): erase-the-NORMAL-FORM lifts the ban for inner ops that compute away.** When the stored body erases tainted, emitDefs retries on the NbE normal form; if the inner ops reduced off it deploys (sound — the normal form is definitionally equal and taint-free), else stays banned (only ADDS sound deployments). Now deploys + RUNS: transp-const ~> id, papp-of-refl ~> the constant, and castU-through-ua — **ch10 `flipped` runs to `false` under node** (the first inner univalence-transport program that executes; `transportIsNot` is its correctness witness). Still banned: bare path values (pabs/notPath) and any normal form with a residual inner head — genuinely-stuck Class-3 transport deploy stays the R-FILL/A1 + open-face glue Repr research tail.

- **B6** (multi-backend source emitters, ahead of the IR refactor) ⇐ Phase-7 erased IR — **FIVE LANDED beyond JS: Python, Go, Rust, BEAM, and JVM (Java 25+).** The JVM backend (`codegen/jvm.go`, target `jvm`, aliases java/java25/javac) targets JVM 25+ idioms — a SEALED interface `V` with RECORD variants (unit/int/closure/ctor/pair), application/projection/show/nat dispatch by PATTERN-MATCHING SWITCH, and a VIRTUAL THREAD entrypoint whose growable stack absorbs deep erased recursion (no 512 MB OS thread). Compiles `javac --release 25`, runs `java -cp <dir> main`; the toolchain is resolved explicitly (asdf temurin-25 glob) so it works under an older PATH `java`. Cross-backend conformance includes JVM: the full corpus (ch04/06/32/39/43 — data/quotient/Σ/partial/IO) observes byte-identical stdout, and FFI (ch44 `hostId`, host method injected into class `main`) links + runs — so the JVM backend reaches FULL feature parity (pure + partial + effects + FFI). The CLI `rune run --target jvm` works. All four backends (`codegen/{js,py,go,rust}.go`) consume the SAME Phase-7 erased IR — no wait on B1/B2's IR refactor, because the current curried `Ir` already suffices for source emitters (only the modules/objects targets, LLVM/Cranelift/WASM, need the reshaped Backend + ABI). Selectable via the `codegen.ByTarget` registry and the CLI `rune emit|run <file> [name] --target js|py|go|rs` (aliases `python`/`rust`/`golang`/`javascript`). Cross-backend conformance (`harness/backend_conformance_test.go`) is the portability gate: the SAME checked program (ch04/ch06/ch32/ch39) observes byte-identical results on every backend — Rust compiles via `rustc --edition 2021` then runs. Python = curried lambdas / dict ctors; Go = `any` + thunks (no init cycles); Rust = `Rc<V>` dynamic enum + `Rc<dyn Fn>` closures that clone forwarded captures to stay `Fn`, large-stack run thread. B2's IR refactor then folds these onto `LowerElim` (deletes the per-backend `emitElim`); B6 is the proof the IR is target-adequate.

(B3+ fan-out — BEAM (Core Erlang) and JVM 25+ both LANDED; a Swift source-emitter port would follow the same B6 pattern. LLVM/Cranelift/WASM-machine additionally need the research-deferred closure heap + apply routine + GC.)

### Track D
- **D1** (proven foundations: Bool/Option/Result, Order, algebra, numeric tower) ⇐ C1, C2 — **unblocked + proven-tier growing.** Landed: algebra-records (ch27), Option (ch29), Result (ch30), arithmetic induction (`addZeroR`/`addSuccR`, ch35), and **the numeric-tower binary laws — `addAssoc` (associativity) + `addComm` (commutativity) of Nat addition, proven by induction (ch63)** — the CommutativeMonoid laws for (Nat,+,0), the substrate D2 sorting/Map-Set + C2 typeclasses ride on. **+ DECIDABLE ORDER (ch65): `lte` (≤) on Nat with `lteZero`/`lteRefl`/`lteSucc` proven + `min` with `minZeroL` + `lteAddR` (a ≤ a+b, order×arithmetic monotonicity) — the comparison D2 sorting/balanced-tree rebalancing needs.** **+ THE SEMIRING STEP (ch63): multiplication `mul` (recurses on first arg) with `mulZeroR` (definitional collapse to the IH) + RIGHT DISTRIBUTIVITY `mulDistR` (`mul (add a b) c = add (mul a c) (mul b c)`, by induction + `addAssoc`) — with the additive commutative monoid this certifies (Nat, +, *, 0, 1) a commutative SEMIRING, the algebra D2's polynomial/matrix code + the typeclass Ring instance ride on.** Still waits on: `lteTrans`/antisymmetry (the triple-nested casing proof), `mulComm`/`mulSuccR` (the add-AC rearrangement), the Monoid…Field instance packaging, the rest of the proofs.
- **D2** (verified collections) ⇐ D1, C4 — **unblocked** (per R-PART): mergesort/quicksort/Map-Set rebalancing ship as `partial` leaves then promote via wfRec. **Substrate landed: ch36 (length is a monoid homomorphism) + ch67 (Lists form a MONOID — `appendNil` right-unit + `appendAssoc`, proven by induction).** Still waits on: `lteTrans` for sortedness proofs, C1/R-SUM for record-shaped Map/Set.
- **D4** (Math/Stats/AI/ML/Plotting interop) ⇐ R-INTEROP, B4, D3 — **unblocked** for fixed-shape arrays, the CArray CRepr, BLAS dirty-NIF binding, dtype/contiguity/finiteness guards, shape-discharge, assume-tracked math. Still waits on: B4/R-FFI, D3 (Double/f64 element type + LA signatures), C1/R-SUM (existential shapes), D1 (decidable dtype/shape equality), B2+B3/BEAM.
- **D5** (OTP-class concurrency, verified) ⇐ R-OTP, C3, B3(BEAM) — **Layer R0 ready** (five more IForeign ops + Pid former + BEAM binding + JS/Go cooperative scheduler shim) on R-EFFECT/C3 + B3; **R1** (typed actor + loop) ready on R-PART/C4; **R2** safety guarantees (intensity-bound, strategy-fidelity) ready on R-SUM/C1. Still waits on: R-COIND/C5 for unbounded loops + the liveness guarantee.
- **D6** (IO/OS/net/time/fs) ⇐ C3, B4 — **BASIC VOCABULARY (in+out+time) LANDED (ch210/ch211).** The standard OS/IO primitive vocabulary now ships WITH the compiler — `printNat : Nat -> IO Nat` (console out), `getNat : IO Nat` (stdin in), `timeNanos : IO Nat` (the OS clock) are baked into each backend runtime as real host bodies (codegen/ioprims.go `ioPrims`/`usesForeign`; js/py/go/rust/beam), exactly as D5's OTP primitives get beamOTPRuntime — NO test injection, so `rune run` executes them unaided. ch210 reads the clock (discarded for determinism) then prints 1 and 2 → byte-identical `1\n2\n2`; ch211 echoes a stdin number (feed "7" → `7\n7`); both on all 5 source backends (harness/io_os_test.go). The `builtin nat` (C7) is load-bearing: a clock reading is ~1e18 ns, a native int, never a succ-chain. Still waits on: D1 (Result/IOError) + B4 (String/Ptr marshalling) for richer fs/net — these basic ops are over `Nat`/`IO` only.
- **D7** (verified hot reload / live upgrade) ⇐ R-HOT, D5, B5, C1, content-addressing, A7 — **the dev-loop half is directly unblocked** (REPL/file `:reload` = re-point `names` + re-elaborate dependents + report the change tier; stateless, content-addressing + a dependency walk, no OTP/BEAM). Tiers 0/1 ready (hash equality + existing conversion); tier 2 ready for first-order fibrant state (A7+B5 landed); tier 3 ready (R-SUM landed). Still waits on: D5/R-OTP + B3 for the live-process runtime (`primUpgrade` → BEAM `code_change`); transactional tree-wide upgrade (R-COIND/E2-shaped) and the inner-path→outer-`Eq` invariant bridge are research.

(D3 — reals/floats + LA interfaces ⇐ D1, B4(BLAS): co-developed with R-INTEROP, which supplies the BLAS behind D3's stated laws; gated like D1/B4.)

### Track E
- **E1** (calculus: terms, semantics, typed chans, projection) ⇐ R-CALC, C1 — **ready-to-build the moment C1/R-SUM lands** for the Proc datatype, Step/structural-congruence relation, Lambert failure rules, and the projection. Still waits on: C1 (Sigma) + the R-EFFECT actor-op vocabulary to RUN (can be stated/proved-about without running); typed channels add as QTT-linear binders later.
- **E3** (verified protocols + projection) ⇐ E2, D5 — **the CRDT G-Counter SAFETY artifact is ready-to-build the moment R-SUM/C1 lands** (Σ + the v2 Bag/Quot multiset + D1/D2 Nat/Vec, zero coinduction); consensus safety (inductive Reachable) is ready alongside it. Still waits on: E2/R-COIND for liveness, D5/R-OTP + B3 for the runtime, the deep per-protocol adequacy proof.

(E2 — bisimulation/equivalence library ⇐ R-CALC, C5, E1: **the cubical correctness currency is LANDED** — `bisimToPathStr` (bisimilarity ⟹ path for `Str A`, computing, no postulate; [NUCONS-DESIGN](NUCONS-DESIGN.md)) + ch66's forward half give the full `pathF (Str A) ↔ (head-path × tail-path)` correspondence, already consumed by E3 trace-equality (ch70). The library packaging (general LTS bisimulation API over R-CALC `Proc`) is the remaining surface work. E4 — infra-as-code surface ⇐ C-INFRA, E3, B3: C-INFRA resolved toward library-first in R-CALC/R-PROTO; needs E3 + B3 + a surface sugar pass.)

### Clarify implement node
- **A8** (overlap mechanism + multi-branch systems) ⇐ C-OVERLAP — **LANDED.** A system over an N-way disjunction is a NESTED fsplit; it dispatches end-to-end by left-biased face-ι. NO new primitive — fsplit (binary-disjunction eliminator) + the face lattice suffice; overlap-agreement is the writer's obligation (C-OVERLAP option b), vacuous for the disjoint atomic faces every Kan wall uses. The "inert until proper-face-hcomp consumer lands" note is cleared (structural hcomp over piF/pathF/sigmaF/Glue + the HIT hcomp-as-constructor are the consumers). Verified: three-way dispatch (system_test.go), ch52_systems.rune; commit c7c0e68, no hash bump.

---

## Still needs more research

Nodes not yet ready-to-build, and what each is waiting on:

- **R-GLUE** — mostly LANDED. R-FILL (`transpFill`), R-BOX (pathF proper-face hcomp), `∀i.φ` (`forallF`), and C-OVERLAP (`fsplit`) all delivered; hcomp/comp-over-Glue and the `glue`-intro transp-over-Glue compute (ch50). The sole residue is the **general** transp-over-Glue arm (neutral input / φ varying in i) — wiring the landed `RestrictIv`+`extend` through the verbatim CCHM `compGlue` (G1, `core/eval.go:2913-2917`). No longer research-blocked; an engineering step.
- **R-UA** — A4 (`pathJ`-via-comp) landed; the derived `castU`-over-`pabsU`-path route computes today. Residue is the postulate-retirement itself (G2): re-body `ua` as the Glue line, delete the baked-in shortcut, take the deliberate `fib`-hash + ch10 event. Depends on G1 for the general transp-over-Glue.
- **R-CALC** — bisimulation needs **R-COIND** (it specifies the codata-by-co-eliminators interface R-COIND must satisfy); to RUN it needs **C1/R-SUM** + the R-EFFECT actor-op vocabulary. The Proc datatype + Step relation + failure rules + projection are stateable/provable now; the equivalence theory is research.
- **R-OTP** — Layer R2's LIVENESS guarantee (coinductive restart-safety over an unbounded fault stream) is blocked on **R-COIND/C5** + **E2**. R0/R1 and R2 safety are ready; R-OTP is a forcing consumer of R-PART + R-COIND + R-SUM together.
- **R-PROTO** — the full node needs **R-SUM** (hard blocker even to state any protocol), **R-COIND** C5a/C5b (all liveness + the projection bridge), **D5/R-OTP + B3/BEAM** (the runtime), and the deep per-protocol **adequacy proof** (mitigated by proving the simple CRDT shape first and assuming BEAM delivery via R-FFI's tracked assume tier). The CRDT safety artifact is ready; liveness/adequacy are research.

Open sub-questions inside otherwise-ready nodes:
- **R-FRAME / X1** — must confirm or refute that Glue/ua reduction never branches on proof-irrelevant equivalence content; that decision sets whether the cache stays fully precise or ships behind `markImprecise`. Blocks fully-precise A6/A7.
- **R-FILL / C-REG** — non-regular CCHM is shipped on the bet that canonicity is provable without regularity; **X2 canonicity tests** are the gate, with Swan's regularity fix adopted only on failure.
- **R-COIND** — C5a + C5b + clocks/`force` + `gfix` + fmapF-over-Later + `forceD` + `gfixF` + the universe-level ▹κ application `laterApp` all LANDED (ch62/ch68/ch69). The converse's primitives are now all in: `gfixF` states the bisimilarity relation, `laterApp` puts its UF-valued recursive occurrence under ▹κ (ch69 `gRelStep` is the abstract Bisim shape, unfolding by refl), `forceD` cashes delayed tails, `dfix`/`force` build/observe. The concrete stream `gBisim` is BUILT (ch69), all combinators landed (`lapD` etc.), and the coinductive destructors `bisimHead`/`bisimTail` are landed. FINDING: the path-assembly endpoint coherence is NOT definitional, so the converse needs Kan endpoint-repair (hcomp over gStr) — a cubical construction. The ENABLER is landed (hcomp over a gfix-typed value unfolds to its sigmaF and fires the structural-Σ Kan rule; TestHcompOverGfixUnfolds). Remaining for the converse: the `Bisim ≃ pathF` path-assembly via hcomp-repair + the `Str ≃ ∀κ. gStr` bridge. A clock-irrelevance axiom is gated on whether a propositional constancy gap bites.
- **R-ERASE2** — deploy of genuinely-stuck Class-3 transports is a kernel **R-FILL/A1** gap (not an erasure gap); open-face glue-value runtime Repr is downstream **R-GLUE/A6**.

Roadmap research elements with **no node in this fan-out**:
- **R-UFH** (fibrant universe hierarchy UF_i) — roadmap-flagged "only if stdlib math needs codes-for-UF / univalence one level up"; no implement node (A10) currently depends on a delivered R-UFH.
